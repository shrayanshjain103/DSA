class Solution{
    static int evenlyDivides(int N){
        // code here
        int x = N; int count = 0;
        while(x!=0){
            int rem=x%10;
             x = x/10;
            if(rem>0 && N%rem==0){
            count++;
           }
        }
        
        return count;
    }
}
